{
  "name": "strategy-kit",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Closed-set call-strategy registry, fixed-comparison scoring, and a hash-pinned approval gate for narrow, bounded model calls (classification, schema-grounded extraction) -- the shared abstraction generalizeFindParams.ts and classifyWithEscalation.ts each hand-rolled independently.",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
